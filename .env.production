# 프로덕션 빌드(Vercel)에서만 적용된다. `npm run dev`(로컬)는 .env 를 써서 목(MSW) 그대로.
# 실서버에 붙는다: MSW를 끄고, /api 요청은 vercel.json rewrite가 hanium 백엔드로 중계한다.
# (VITE_API_BASE_URL 은 .env 의 /api 를 그대로 상속 → 같은 출처 요청이라 CORS 불필요.)
VITE_USE_MOCK=false
